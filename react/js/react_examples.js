// los elementos en React son inmutables.
const element = <h1>Hello, world</h1>;
ReactDOM.render(element, document.getElementById('root'));

// All React components must act like pure functions with respect to their props.
